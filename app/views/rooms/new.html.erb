<h1>Creating a room</h1>
<%= simple_form_for [@team, @room] do |form| %>
  <%= form.input :name %>
  <%= form.submit "Save", class: 'btn btn-success' %>
<% end %>
<%= link_to "Back to Team", team_positions_path(@team)  %>
